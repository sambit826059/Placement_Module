import React, { useEffect, useState } from 'react'

export default function AuthUserType({ SendDataToApp, UserType }) {
    useEffect(() => {
      // SendDataToApp(UserType);

    }, [SendDataToApp, UserType]);    
  return (
    <>
    </>
  )
}
